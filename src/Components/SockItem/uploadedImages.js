import { S3_URL } from "config";

const nike = `${S3_URL}/Nike.png`;
const apple = `${S3_URL}/apple.png`;
const weCodeLogo = `${S3_URL}/wecodelogo.png`;
const plus = `${S3_URL}/addImage.png`;

export { nike, apple, weCodeLogo, plus };
